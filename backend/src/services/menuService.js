const db = require('../models');

exports.syncMenus = async (locationId) => {
  // Implement logic to sync menus with third-party integrations
  // Example: Send updated menu to providers like DoorDash or Uber Eats
};
